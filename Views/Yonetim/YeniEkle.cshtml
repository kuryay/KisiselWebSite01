@model Portfolio
@{
    ViewData["Title"] = "YeniEkle";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<style>
    input[type="file"] {
        cursor: pointer !Important;
        font: 300 14px sans-serif;
        color: #9e9e9e;
    }

        input[type="file"]::-webkit-file-upload-button {
            font: 300 14px sans-serif;
            background: #009688;
            border: 0;
            padding: 12px 25px;
            cursor: pointer;
            color: #fff;
            text-transform: uppercase;
        }

        input[type="file"]::-ms-browse {
            font: 300 14px 'Roboto', sans-serif;
            background: #009688;
            border: 0;
            padding: 12px 25px;
            cursor: pointer;
            color: #fff;
            text-transform: uppercase;
        }

</style>

<form asp-controller="Yonetim" asp-action="YeniEkle" method="post" enctype="multipart/form-data">

    <center>
        <div class="form-group col-md-6 text-center">
            <label>Başlık</label>
            <input class="form-control" asp-for="Title" placeholder="Başlık Giriniz..." />
        </div>
    </center>

    <center>
        <div class="form-group col-md-6 text-center">
            <label>Açıklama</label>
            <textarea class="form-control" asp-for="Description" rows="8" placeholder="Açıklama Giriniz..."></textarea>
        </div>
    </center>

    <center>
        <div class="form-group col-md-6 text-center">
            <label>Başlık</label>
            <input class="form-control" type="file" name="Image" />
        </div>
    </center>

    <center><button type="submit" class="btn btn-info"><i class="fas fa-plus-circle"></i> Yeni Ekle</button></center>

</form>

